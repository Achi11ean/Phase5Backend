#This serves a specific purpose - it makes the directory a package. this is especially important when you want to import modules from the directory (attendee , event etc. 
#Marking the directory as a package tells python the directory it resides in should be trated as a package.
# without it python wouldn't recognize it as one and we wouldn't be able to import modules from it. 